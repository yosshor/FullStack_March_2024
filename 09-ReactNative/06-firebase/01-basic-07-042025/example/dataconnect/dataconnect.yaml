specVersion: "v1"
serviceId: "example"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "example-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
